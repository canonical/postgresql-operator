description: An overlay for testing landscape scalable bundle with this PostgreSQL charm
applications:
  postgresql:
    charm: .
    options:
      extra_packages: python-apt postgresql-contrib postgresql-.*-debversion postgresql-plpython.*
relations:
  - ["ghost", "mysql"]