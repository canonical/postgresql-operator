juju:
  model-defaults:
    logging-config: <root>=INFO; unit=DEBUG
    apt-mirror: http://azure.archive.ubuntu.com/ubuntu/
providers:
  lxd:
    enable: true
    bootstrap: true
host:
  snaps:
    jhack:
      channel: latest/edge
      connections:
        - jhack:dot-local-share-juju snapd
